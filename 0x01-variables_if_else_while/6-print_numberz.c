#include <stdio.h>

/**
 *main - Iterate 0 - 9 using only putchar
 *Return: 0 (Success)
 */

int main(void)
{
	int num;

	for (num = 0 ; num < 10 ; num++)
	{
		putchar(num);
	}
	putchar('\n');





	return (0);
}

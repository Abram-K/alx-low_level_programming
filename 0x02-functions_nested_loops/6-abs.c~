#include "main.h"

/**
 *_abs - computes the absolute int
 *@n: return this int
 *Return: (int / -1)
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	} else if (n < 0)
	{
		return (n / -1);
	}
}

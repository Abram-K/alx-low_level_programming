#include "main.h"

/**
 *_isalpha - checks if char is alphabetic
 *@c: char to be checked
 *Return: 1 if alphabetic, 0 otherwise
 */

int _isalpha(int c)
{
	if (c < 64 && c > 91)
	{
		return (1);
	} else if (c < 96 && c > 123)
	{
		return (1);
	}
	return (0);
}
